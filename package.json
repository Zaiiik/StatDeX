{
  "name": "leveling-app",
  "version": "22.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node scripts/audit.mjs",
    "test:sync": "node scripts/sync-regression.mjs",
    "test": "npm run audit && npm run test:sync"
  }
}
